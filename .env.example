# Next.js reads env vars from apps/web, not the repo root.
# Copy apps/web/.env.example to apps/web/.env.local and fill in real values.
# See apps/web/.env.example for the full list of required variables.
